# TelemetryForge Super Widgets

Super Widgets are manifest-driven telemetry components rendered as one
movable and resizable object.

Each component lives in its own folder and contains a `manifest.yaml`. The
initial `cpu-command-dial` component uses the built-in `cpu_command_dial`
renderer template and binds CPU clock, temperature, utilisation and fan speed
automatically.
